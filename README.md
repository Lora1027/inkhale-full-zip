# Inkhale Pro (Next.js + Supabase)
Features: login, filters, **add/edit/delete transactions**, prettier Comparison charts, **printable Reports**, inventory CSV bulk upload.
See `schema.sql` for DB setup. Add env vars in Vercel: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`.
